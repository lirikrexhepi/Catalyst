# Catalyst

A native desktop application built with Go and React using the [Wails](https://wails.io/) framework.

## Live Development

To run in live development mode, run `wails dev` in the project directory, or use `dev.bat` on Windows.

## Building

To build a redistributable, production package, run `wails build`.
